# 진행 상황

최종 갱신: 2026-08-06 · 커밋 `18db4a2`

## 지표

| 항목 | 상태 |
| --- | --- |
| 테스트 | 124개 통과 (node 유닛 + chromium 브라우저) |
| `check:types` | 통과 |
| `check:i18n` | 통과 (missing / invalid / unused / undefined 없음) |
| `lint` | 신규 코드 0건 |
| `check:deps` (knip) | `lefthook` 하나만 — 보일러플레이트 원본 상태 |
| `build` | 통과 |
| 배포 | GitHub `main` push → Vercel 프로덕션 자동 배포 |

## 완료

### 라벨 엔진

- **FR-01 혼용률 파싱** — 자유 입력 → 법정 표준 소재명. 별칭 80여 개.
  PRD의 단일 정규식 대신 토크나이저를 씁니다: `[^0-9]*`가 한글을 삼켜서
  `겉감: 면 80%`를 `{겉감: 80}`으로, `80% 면 20% 폴리`를 한 칸씩 밀려 읽는
  문제가 있었습니다. 커버리지 97%+
- **FR-02 세탁 기호 룰 엔진** — KS K 0021 4단계 우선순위. 커버리지 100%
- **Code 128 / QR** — 라이브러리로 인코딩만 계산하고 벡터 사각형으로 직접 그림

### 스튜디오

- 6개 카테고리 **17개 템플릿** + 빈 캔버스 (썸네일은 실제 문서 데이터를 SVG로 렌더)
- **캔버스 직접 편집** — 클릭 선택, 드래그 이동, 핸들 리사이즈, 텍스트 더블클릭
  제자리 수정, Delete 삭제, Esc 선택 해제
- **작업 공간** — mm 눈금자, 줌(+/−/맞춤), 스크롤
- **페이지 속성** — 가로·세로 직접 입력, mm/px/inch 단위 전환, 기공 프리셋 5종
- **되돌리기 / 다시 실행** — `Ctrl+Z` / `Ctrl+Shift+Z`, 60단계. 입력창 포커스 중엔
  단축키가 물러나고, 같은 필드 연속 편집은 700ms 안에서 한 항목으로 합쳐짐
- **좌측 패널 ↔ 캔버스 양방향 바인딩** — 같은 스토어를 쓰므로 어긋날 수 없음
- **이미지 요소** — 파일 업로드 → data URL로 문서에 인라인 저장 (1MB 제한)

### 출력

- **인쇄용 벡터 PDF** — 문서의 mm 크기가 그대로 PDF 페이지 크기가 됨.
  30×70mm → MediaBox `[0 0 85.039 198.425]`pt 로 검증. 텍스트는 텍스트로,
  바코드·QR은 벡터 경로로. 한글 폰트(Nanum Gothic) 임베드

### 인프라

- ko(기본) / en / fr 3개 로케일, canonical·hreflang·sitemap
- Clerk 로그인·회원가입 유지 (DB·대시보드는 제거)
- GitHub → Vercel 자동 배포

## 남은 작업 (우선순위 순)

1. **문서 저장·불러오기 (JSON 내보내기/가져오기)** — 아래 "가장 큰 제약" 참고.
   현재는 브라우저를 지우면 작업물이 사라집니다.
2. **하단 플로팅 도구모음** — 선택/텍스트/도형/펜 도구를 캔버스 하단 중앙에.
   현재는 좌측 패널의 `+ 요소 추가` 버튼
3. **여러 페이지** — 하단 페이지 썸네일 + 새 페이지. `LabelDocument`를
   `pages: LabelDocument[]`로 바꾸는 모델 변경이 필요. 되돌리기가 문서 스냅샷
   방식이라 페이지 추가·삭제도 자동으로 커버됨
4. 좌측 아이콘 레일, 배경색 지정, PNG 내보내기, 레이어 순서 변경(위/아래로 보내기)

## 알려진 제약

### 가장 큰 제약: 작업물이 브라우저에만 있습니다

문서는 `localStorage`에만 저장됩니다. **브라우저 데이터를 지우거나 다른 기기·
브라우저로 옮기면 작업물이 사라집니다.** 시크릿 모드에서도 창을 닫으면 사라집니다.

비회원으로 쓸 수 있게 하려는 원래 의도의 결과이지만, 실무에서 라벨 하나를 몇 번씩
고쳐 쓰는 흐름에는 위험합니다. 해결책 두 가지:

- **JSON 내보내기·가져오기** (남은 작업 1번) — 계정 없이도 파일로 백업. 문서가
  이미 순수 데이터라 직렬화가 그대로 됩니다. 작업량이 작고 효과가 큽니다.
- 계정 기반 서버 저장 — Clerk을 실제로 연결하고 저장소를 붙이는 큰 작업

### 법적·규제 관련 — 실제 인쇄 전 반드시 교체

- **KC 마크가 자리표시용 점선 도형입니다.** KC 마크는 등록된 지정 도형이라 임의로
  그린 것을 제품에 붙이면 규제 위반입니다. `image` 요소로 정품 로고 파일을 올려
  교체하세요.
- **세탁 기호 아이콘이 근사 도형입니다.** 매칭 로직은 KS K 0021 우선순위를 따르지만
  아이콘 자체는 공인 아트워크가 아닙니다. 법적 의미는 옆에 인쇄되는 한글 문구가
  담고 있습니다. [careSymbolShapes.ts](src/features/label/careSymbolShapes.ts)의
  path만 교체하면 세 렌더러에 동시 반영됩니다.

### 기타

- **Clerk 키가 자리표시자입니다.** `/sign-in`·`/sign-up`은 열리지만 실제 인증은
  동작하지 않습니다. 커밋된 publishable key는 보일러플레이트의 공유 데모
  인스턴스(제3자 소유)입니다. 본인 Clerk 앱의 키 두 개를 Vercel 환경변수에 넣으면
  해결됩니다.
- **이미지 요소는 PDF에서 래스터입니다** (원본이 래스터이므로 당연). SVG를 올렸을
  때 PDF에서 벡터로 유지되는지는 확인하지 않았습니다.
- **커스텀 도메인 미연결** — 현재 `260804tag.vercel.app`. 도메인을 준비하면
  `vercel domains add` → DNS 등록 → `NEXT_PUBLIC_APP_URL` 갱신 순서입니다.
- en/fr의 `Features`·`FAQ` 문구는 아직 보일러플레이트 영문입니다 (ko는 제품 문구).
